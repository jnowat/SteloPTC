/// <reference types="svelte" />
/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected at build time via `define` in vite.config.ts (package.json version).
declare const __APP_VERSION__: string;
